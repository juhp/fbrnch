resolver: lts-22.43
ghc-options:
  HsOpenSSL: -optc=-Wno-incompatible-pointer-types
extra-deps:
  - pagure-0.2.1
  - fedora-krb-0.1.0
  - fedora-releases-0.3.0
  # workaround local email-validate failure:
  - os-string-2.0.6
  - select-rpms-0.3.0
