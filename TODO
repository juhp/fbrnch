- tag builds locally
- scratch/build: show total build times
- filter bz# out from bodhi notes
- convert: to rpmautospec
- layers command to print

- config option to cache tarballs in ~/rpmbuild/SOURCES if it exists when _sourcedir is cwd

- check envr > any existing current package, before building
- use rpm-nvr: eg for nameOfNVR
- 'clean' command
- merge rawhide pushes to local main?
- use +/+ for urls instead of </>
- pull creating new branches and/or clone --all?
- stash and restore

- push/build a commit (eg if one doesn't want to build HEAD yet)
- merge koji-progress
- install from koji (koji-install)

# new commands
- 'unpushed' command, etc (from f-h-t)
- 'push'
- 'check-sources'
- worktree support
- 'update-sources'
- 'clean-old-source-dirs' (--threshold SIZE)
- 'chain'
- 'check-deps'
- 'clone-deps'
- 'remove-sidetag(s)'

# release deps on hackage:
- pagure (new)
- bodhi (new)
- copr (new)
- fedora-dists (update)
  - pdc (new)

# other namespaces
- support containers/ and modules/

# multipackage
- build sequence of packages by branch
- build: auto override if dep
- multipackage update
- use sidetag
  - combines release updates into single bodhi update

# misc
- unify checking for pagure and git remote branches
- remove ifM's?

# auth
- update pagure key in ~/.config/rpkg/fedpkg.conf
- bugzilla login via api key

# status/*unbuilt*/unreleased
- list pending new package builds
- list builds not pushed to bodhi
- status when no local dir/checkout

# longterm ideas
- "fbrnch todo" list
- local state caching
- use hs-git
- rpminspect
